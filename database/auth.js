// auth.js

// Definición de la función generateAuthToken
function generateAuthToken(user) {
    // Lógica para generar un token de autenticación
    return token;
  }
  
  // Exporta la función generateAuthToken para que pueda ser utilizada en otros archivos
  module.exports = {
    generateAuthToken
  };